"""Reference implementations behind the golden fixtures of kestrel-chartkit.

Everything here is written from the documented formulas, independently of the Rust code it
checks, and uses the Python standard library only. `reference/generate.py` enforces the latter.
"""
