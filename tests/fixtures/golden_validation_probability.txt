# kestrel-chartkit golden reference fixture: validation and probability calibration
#
# Generated by reference/generate.py from reference/kestrel_reference/fixtures/validation_probability.py:
# an independent second implementation written from the documented formulas, using the
# Python standard library only. `python3 reference/generate.py` regenerates this file;
# `python3 reference/generate.py --check` verifies it byte for byte.
#
# Predictions [0.8, 0.7, 0.4, 0.1] for the outcomes [win, win, loss, loss]. Log-loss is the
# mean of -ln p for a win and -ln(1 - p) for a loss, p clamped to [1e-15, 1 - 1e-15]:
# -(ln 0.8 + ln 0.7 + ln 0.6 + ln 0.9) / 4.
#
# validation_probability_tolerance is the comparison tolerance the tests apply, stated rather
# than derived.

# calibration sample
log_loss=0.2990011586691898

validation_probability_tolerance=1e-12
